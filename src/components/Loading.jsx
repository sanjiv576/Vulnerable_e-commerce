import { Height, Widgets, WidthFull } from '@mui/icons-material'
import React from 'react'

export const Loading = () => {
    return (
        <div>
            <span style={{ height: '50%', width: '40%' }} className="loading loading-spinner loading-sm">Loading</span>

        </div>
    )
}
